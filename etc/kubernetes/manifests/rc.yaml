apiVersion: v1
kind: ReplicationController
metadata:
  name: example
spec:
  replicas: 2
  selector:
    app: santander-phpinfo
  template:
    metadata:
      labels:
        app: santander-phpinfo
    spec:
      containers:
        - name: php
          image: index.docker.io/rodrigollanos1/phpinfo:latest
          ports:
            - containerPort: 8080
